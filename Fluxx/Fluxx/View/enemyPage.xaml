<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fluxx.View.enemyPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Label Text="Mano enemiga" HorizontalOptions="CenterAndExpand"/>
            <ListView x:Name="keeperListView"
                      ItemSelected="keeperListView_ItemSelected"
                      ItemTapped="keeperListView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}"/>
                                <Label Text="{Binding Nametype}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="Meta" HorizontalOptions="CenterAndExpand"/>
            <Label x:Name="GoalName" HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>